'use strict';

angular.module('GSTApp')
.controller("resendsuccessCtrl", ['$scope', '$state', '$timeout', '$window', 'ApiFactory', 'toasterService', function (scope, state, timeout, window, ApiFactory, toasterService) {
   
}]);